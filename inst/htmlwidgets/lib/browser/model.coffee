# --- DataSet ----------------------------------------------------------
DataSet = (raw) ->
  
  dataset = () ->

  return dataset


# --- exports ----------------------------------------------------------

window.DataSet = DataSet
